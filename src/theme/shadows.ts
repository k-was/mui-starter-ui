import { Shadows } from '@mui/material/styles';

const gold = '200, 165, 92';

const shadows: Shadows = [
  'none',
  `0 1px 2px rgba(${gold}, 0.04)`,
  `0 2px 4px rgba(${gold}, 0.04)`,
  `0 3px 6px rgba(${gold}, 0.04)`,
  `0 4px 8px rgba(${gold}, 0.05)`,
  `0 5px 10px rgba(${gold}, 0.05)`,
  `0 6px 12px rgba(${gold}, 0.05)`,
  `0 7px 14px rgba(${gold}, 0.06)`,
  `0 8px 16px rgba(${gold}, 0.06)`,
  `0 9px 18px rgba(${gold}, 0.06)`,
  `0 10px 20px rgba(${gold}, 0.07)`,
  `0 11px 22px rgba(${gold}, 0.07)`,
  `0 12px 24px rgba(${gold}, 0.07)`,
  `0 13px 26px rgba(${gold}, 0.08)`,
  `0 14px 28px rgba(${gold}, 0.08)`,
  `0 15px 30px rgba(${gold}, 0.08)`,
  `0 16px 32px rgba(${gold}, 0.09)`,
  `0 17px 34px rgba(${gold}, 0.09)`,
  `0 18px 36px rgba(${gold}, 0.09)`,
  `0 19px 38px rgba(${gold}, 0.10)`,
  `0 20px 40px rgba(${gold}, 0.10)`,
  `0 21px 42px rgba(${gold}, 0.10)`,
  `0 22px 44px rgba(${gold}, 0.11)`,
  `0 23px 46px rgba(${gold}, 0.11)`,
  `0 24px 48px rgba(${gold}, 0.12)`,
];

export default shadows;
